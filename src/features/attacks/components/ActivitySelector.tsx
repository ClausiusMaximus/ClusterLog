import { AppCard } from "@/components/common";
import Typography from "@mui/material/Typography";

export default function ActivitySelector() {
  return (
    <AppCard>
      <Typography>Tätigkeit</Typography>
    </AppCard>
  );
}