import { AppCard } from "@/components/common";
import Typography from "@mui/material/Typography";

export default function SideSelector() {
  return (
    <AppCard>
      <Typography>Kopfseite</Typography>
    </AppCard>
  );
}