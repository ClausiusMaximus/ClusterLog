# Changelog

## Unreleased
- Git-Workflow für `main`, `dev`, `feature/*`, `bugfix/*` und `release/*` etabliert
- Projekt-Dokumentation für Roadmap, Contributing und Bug-Tracking ergänzt

## v3.0
- Dokumentation generiert
- Projektstruktur und Entwicklungsdokumentation erweitert
## Unreleased maintenance

- Removed the unused `AttackService` pass-through layer; callers now use the existing repository directly.
- Removed unsafe `any` usage in backup import and statistics typing without changing the backup format.
- Restored a clean ESLint result for the application source.
